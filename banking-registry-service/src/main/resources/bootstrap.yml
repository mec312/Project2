spring:
    application:
        name: banking-registry-service
    cloud:
        config:
            uri: http://localhost:8081
            username: nttdata
            password: nttd4t4
        gateway:
            discovery:
                locator:
                    enabled: true

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false